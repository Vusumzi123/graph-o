function funct(formula, x,t){
 return eval(formula.toLowerCase())*(-1);
};

//add extended math library

function distance(x1,x2,y1,y2){
  return Math.sqrt((Math.pow(x2-x1,2))+(Math.pow(y2-y1,2)));
};

function cos(t){
  return Math.cos(t);
};

function sin(t){
  return Math.sin(t);
};

function sen(t){
  return Math.sin(t);
};

function tan(t){
  return Math.tan(t);
};

function pow(x,p){
  return Math.pow(x,p);
};

function pi(){
  return Math.PI;
}

function e(){
  return Math.E;
}
